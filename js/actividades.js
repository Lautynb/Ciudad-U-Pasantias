function buscarPorTema() {
    // Aquí puedes agregar la lógica para manejar la búsqueda por tema/carrera/universidad
    var tema = document.getElementById("temaInput").value;
    // Realiza la acción deseada con la entrada del usuario (tema)
    console.log("Buscando por tema:", tema);
}